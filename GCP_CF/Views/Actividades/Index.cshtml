@model IEnumerable<GCP_CF.Models.Actividades>

@{
    ViewBag.Title = "Index";
}

<h2>Actividades</h2>

<p>
    @Html.ActionLink("Crear Nueva", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripción)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripción)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Actividad_Id }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Actividad_Id }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Actividad_Id })
        </td>
    </tr>
}

</table>
