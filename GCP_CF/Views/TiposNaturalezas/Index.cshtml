@model IEnumerable<GCP_CF.Models.TiposNaturaleza>

@{
    ViewBag.Title = "Index";
}

<h2>Tipos Naturaleza</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Naturaleza_Id }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Naturaleza_Id }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Naturaleza_Id })
        </td>
    </tr>
}

</table>
